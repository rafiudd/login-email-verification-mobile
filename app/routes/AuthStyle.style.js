import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
   viewStack: {
    flexDirection: 'row', 
    marginLeft: 13 
   },
   iconBackHeader: { 
    height: 30, 
    width: 30 
}
});

export default styles;